
Assuming, that what we want is possible:
- Get list of all branches
- allow filtering via regex
- If the branch points to a commit that is already in the master branch, this branch is merged and can be ignored
- The remainder are branches that we want to list
